# Copiar como .env.dev, .env.test, .env.staging o .env.prod
# NUNCA commitear archivos .env con valores reales

# Entorno
ENV=dev
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Base de datos
DB_PATH=data/dev/finanzas_dev.db

# Rutas (Windows: usar forward slashes o escapar backslashes)
ONEDRIVE_PATH=C:/Users/ghriz/OneDrive/Finanzas MCGHR
BACKUP_PATH=data/dev/backups

# Claude API
ANTHROPIC_API_KEY=sk-ant-...
CLAUDE_PROVIDER=mock   # real | mock

# Mail
MAIL_PROVIDER=mock     # real | mock
OUTLOOK_APP_PASSWORD=

# CORS (separados por coma)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
